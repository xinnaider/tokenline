# OS
.DS_Store
Thumbs.db

# Editors
*.swp
*.swo
.idea/

# VS Code: ignore personal files, keep the shared project config
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# Local scratch
*.log

# temporary files
/tmp

# node / build artifacts
node_modules/
dist/
*.tgz

# personal Claude Code overrides (the committed .claude/settings.json is the example)
.claude/settings.local.json

# Git worktrees created via Claude Code's EnterWorktree (each lives under .claude/worktrees/)
/.claude/worktrees
